class Item {
  
  constructor(options) {
    this.icon = options.icon
    this.name = options.name
    this.price = options.price
    this.id = options.id
    this.groupId = options.groupId
  }
  
}

module.exports = Item